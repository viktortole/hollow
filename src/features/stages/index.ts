/**
 * Stages feature — public API.
 *
 * Metabolic stage definitions and visualizations: the 6 stages a user passes
 * through during a fast (Fed → Stem Cell). The icons that orbit the timer ring
 * (Phase 4) live here.
 */

export { STAGE_ICONS, FedIcon, EarlyFastIcon, FatBurningIcon, AutophagyIcon, DeepKetosisIcon, StemCellIcon } from "./stageIcons";
